# Required (3 vars — app panics without these)
MONGODB_URI=mongodb://localhost:27017
MONGODB_NAME=football
SITE_KEY=your-secret-key-change-this

# Optional for email activation (4 vars — email silently fails without)
EMAIL_SMTP=smtp.example.com
EMAIL_FROM=noreply@example.com
EMAIL_USERNAME=
EMAIL_PASSWORD=
